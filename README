This is my mcollective plugins repository
	grep : grep files on nodes
	misc : run misc commands (uptime, reobot..)
	status : queries files at /var/status/*.status (written by your processes to expose state)
